import styled from "styled-components/macro"

import { colors } from "src/theme"

const P = styled.p`
  font-family: "Roboto";
  font-size: 1.5rem;
  font-weight: 100;
  color: ${colors.text};
`

export default P
