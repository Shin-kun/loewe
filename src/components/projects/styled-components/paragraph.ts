import styled from "styled-components/macro"

import Text from "src/components/text"

export const JobOverviewSummary = styled(Text)`
  font-size: 1rem;
  font-weight: 400;
  margin: 0;
`

export const Paragraph = styled(JobOverviewSummary)``
