import styled from "styled-components/macro"

import { colors } from "src/theme"

const H5 = styled.h5`
  font-family: "Montserrat";
  font-size: 2.25rem;
  font-weight: 700;
  color: ${colors.text};
`
export default H5
